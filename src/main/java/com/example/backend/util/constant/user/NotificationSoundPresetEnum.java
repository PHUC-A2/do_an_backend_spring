package com.example.backend.util.constant.user;

/**
 * Kiểu chuông thông báo trên giao diện (do người dùng chọn).
 */
public enum NotificationSoundPresetEnum {
    /** Chuông mặc định — hai xung rõ ràng. */
    DEFAULT,
    /** Âm trầm, nhẹ hơn. */
    SOFT,
    /** Ba tiếng bíp ngắn, dễ chú ý. */
    ALERT
}
