package com.example.backend.util.constant.devicereturn;

/**
 * Tình trạng sau khi dùng — bảng returns (db.md DeviceCondition).
 */
public enum DeviceCondition {
    GOOD,
    DAMAGED,
    BROKEN,
    LOST
}
